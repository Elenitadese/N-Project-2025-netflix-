import React, { useEffect } from "react";
import requests from '../Utils/Requastes.jsx'

const TestFile = () => {
  useEffect(() => {
    // console.log(requests); // This will log the entire requests object
  }, []);

  return <div>...</div>;
};
export default TestFile;
